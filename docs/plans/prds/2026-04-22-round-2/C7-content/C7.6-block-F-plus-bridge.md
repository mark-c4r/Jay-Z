---
prd-id: C7.6
cluster: C7-content
title: Block F+ bridge paragraph (between Block E and Block F)
appetite: S
risk: 2
depends-on: [C7.5]
unblocks: []
pre-state-commit: f1cb7b9
verified-at: 2026-04-22T15:18:01Z
status: completed
codex-reviewed-at: 2026-04-22T16:30:00Z
codex-verdict: APPROVE_WITH_REVISIONS
codex-folded-at: 2026-04-23T00:59:21Z
---

# PRD — C7.6: Block F+ bridge paragraph (between Block E and Block F)

## Sonnet pickup protocol (read first)

```bash
set -e
cd /Users/profile_1/Coding/jarda/jarda-ai-start
pre_state="f1cb7b9"
git merge-base --is-ancestor "$pre_state" "$(git rev-parse HEAD)" || { echo "DRIFT: pre-state"; exit 1; }
test -z "$(git status --short)" || { echo "DRIFT: uncommitted changes"; exit 1; }

grep -q "^status: completed" docs/plans/prds/2026-04-22-round-2/C7-content/C7.5-block-E-growth.md || { echo "DEP UNSATISFIED: C7.5"; exit 1; }

# Growth-opportunities block must be filled (not hidden)
if rg -q 'id="research-growth-opportunities" hidden' index.html; then echo "DRIFT: Block E not yet shipped"; exit 1; fi
rg -c 'id="research-growth-opportunities">' index.html | grep -q '^1$' || { echo "DRIFT: Block E stub not visible"; exit 1; }
rg -c 'id="research-seo-recipes" hidden' index.html | grep -q '^1$' || { echo "DRIFT: Block F stub missing/not hidden"; exit 1; }
test -f /Users/profile_1/Coding/jarda/jarda-ai-start/docs/research/2026-04-21-round-2-research/gear-lists-business-research.md || { echo "DRIFT: gear-lists research missing"; exit 1; }

# No pre-existing research-bridge
if rg -q 'class="research-bridge"' index.html; then echo "DRIFT: research-bridge already present"; exit 1; fi

echo "==> C7.6 pickup PASSED"
```

## Problem

Block F+ is a one-paragraph bridge between Block E (growth opportunities) and Block F (SEO recipes). It points at the gear-content research bundle (`docs/research/2026-04-21-round-2-research/gear-lists-business-research.md`) without inlining it — keeps the primer lean; readers who want depth follow the link.

The bridge is a `<p class="research-bridge">` — NOT a `.research-block` (keeping appendix block count at 7). Placement: AFTER `research-growth-opportunities` closes, BEFORE `research-seo-recipes` opens. Stubs from C7.3 have this gap.

## Verified Assumptions

These VAs describe the expected post-C7.5 pickup state, not the current pre-dependency tree.

| # | Claim | Line ref | Verify command | Expected |
|---|---|---|---|---|
| VA-1 | Block E shipped (growth-opportunities no longer `hidden`) | post-C7.5 | `rg -c 'id="research-growth-opportunities">' index.html` | 1 |
| VA-2 | Block F stub (seo-recipes) still `hidden` (C7.7 hasn't shipped) | post-C7.3 | `rg -c 'id="research-seo-recipes" hidden' index.html` | 1 |
| VA-3 | No pre-existing `.research-bridge` element | — | `! rg -q 'class="research-bridge"' index.html` | exits 0 |
| VA-4 | `gear-lists-business-research.md` exists in research bundle | umbrella | `test -f /Users/profile_1/Coding/jarda/jarda-ai-start/docs/research/2026-04-21-round-2-research/gear-lists-business-research.md` | exists |

## Cross-cluster contracts honored

- **Depends on:** C7.5 shipped Block E.
- **Enforces:** `<p class="research-bridge">` is NOT a `.research-block` (block count stays at 7).
- **Does not conflict with:** C7.7 (Block F) — bridge goes immediately BEFORE the Block F stub.

## Work — exact edits

### Edit 1: Insert bridge paragraph between E close and F open

**File:** `index.html`
**Anchor:** closing `</div>` of `research-growth-opportunities` + opening `<div class="research-block" id="research-seo-recipes" hidden>`
**Action:** insert-after the `</div>` of `research-growth-opportunities`, before the opening `<div>` of `research-seo-recipes`

**Before** (state after C7.5 ship):

```html
          <p>Each of these is sized for a weekend of setup plus ongoing 15-minute-per-week care. None is "build an agent." All are "save 30 minutes a week, 52 weeks a year."</p>
        </div>

        <div class="research-block" id="research-seo-recipes" hidden>
```

**After**:

```html
          <p>Each of these is sized for a weekend of setup plus ongoing 15-minute-per-week care. None is "build an agent." All are "save 30 minutes a week, 52 weeks a year."</p>
        </div>

        <p class="research-bridge">One vertical compounds several of the items above: gear content. A gear list in your voice for each peak turns every blog post into affiliate revenue plus lead-magnet fodder plus a conditions-report asset. The full playbook — peer patterns, three-tier Claude automation, European affiliate catalog — lives in the research bundle at <code>docs/research/2026-04-21-round-2-research/gear-lists-business-research.md</code>. Short version: start with one gear list for one peak, pick two affiliate programs (Bergfreunde.eu + Amazon DE), ship in three weeks, learn what converts, then scale with Claude.</p>

        <div class="research-block" id="research-seo-recipes" hidden>
```

### Edit 2: Add `.research-bridge` CSS

**File:** `index.html`
**Anchor:** immediately after the unique `.research-subtitle` rule in `<style>`
**Action:** insert a new rule

```css
    .research-bridge {
      max-width: 720px;
      margin: 28px auto;
      padding: 16px 20px;
      background: var(--bg-inset);
      border-left: 3px solid var(--muted);
      border-radius: 4px;
      font-size: 14px;
      line-height: 1.6;
      color: var(--ink);
    }
    .research-bridge code {
      font-family: ui-monospace, Menlo, monospace;
      font-size: 13px;
      background: transparent;
    }
    @media (max-width: 480px) {
      .research-bridge { padding: 12px 16px; font-size: 13px; }
      .research-bridge code { font-size: 12.5px; word-break: break-all; }
    }
```

## Copy locks (verbatim)

**Bridge paragraph:**

> One vertical compounds several of the items above: gear content. A gear list in your voice for each peak turns every blog post into affiliate revenue plus lead-magnet fodder plus a conditions-report asset. The full playbook — peer patterns, three-tier Claude automation, European affiliate catalog — lives in the research bundle at `docs/research/2026-04-21-round-2-research/gear-lists-business-research.md`. Short version: start with one gear list for one peak, pick two affiliate programs (Bergfreunde.eu + Amazon DE), ship in three weeks, learn what converts, then scale with Claude.

Punctuation: em-dashes in "playbook — peer patterns", "European affiliate catalog —". No plus-sign arithmetic; use "plus" as prose.

## Acceptance Criteria (block-local)

- [ ] AC-1: `<p class="research-bridge">` exists exactly once
  - Verify: `rg -c 'class="research-bridge"' index.html` → 1
- [ ] AC-2: Bridge sits between `research-growth-opportunities` and `research-seo-recipes`
  - Verify: `rg -n '(id="research-growth-opportunities"|class="research-bridge"|id="research-seo-recipes")' index.html | head -4` — line order: growth-opportunities, research-bridge, seo-recipes
- [ ] AC-3: Bridge is NOT a `.research-block`
  - Verify: `rg -c '<p class="research-bridge"' index.html` → 1
  - Verify: `rg -c 'class="research-block"' index.html` → 7 (unchanged)
- [ ] AC-4: Bridge cites gear-lists research filename
  - Verify: `rg -c 'gear-lists-business-research\.md' index.html` → 1
- [ ] AC-5: Two affiliate programs named
  - Verify: `sed -n '/class="research-bridge"/,/id="research-seo-recipes"/p' index.html | rg -o "Bergfreunde\.eu|Amazon DE" | wc -l` → 2
- [ ] AC-6: Word count 80–140
  - Verify: `rg "class=\"research-bridge\"" index.html | head -1 | wc -w` — this inline wordcount is tricky; use: `sed -n '/class="research-bridge"/,/research-seo-recipes/p' index.html | head -3 | wc -w` in [80, 140]
- [ ] AC-7: `.research-bridge` CSS present
  - Verify: `rg -c '^    \.research-bridge \{' index.html` → 1

## Verify (end-to-end post-build)

```bash
cd /Users/profile_1/Coding/jarda/jarda-ai-start

rg -c 'class="research-bridge"' index.html                                   # 1
rg -c 'class="research-block"' index.html                                    # 7 (no change)
rg -c 'gear-lists-business-research\.md' index.html                          # 1
sed -n '/class="research-bridge"/,/id="research-seo-recipes"/p' index.html | rg -o "Bergfreunde\.eu|Amazon DE" | wc -l  # 2
rg -c '^    \.research-bridge \{' index.html                                 # 1

# Placement order
rg -n '(id="research-growth-opportunities">|class="research-bridge"|id="research-seo-recipes")' index.html

# Invariants
rg -c '<section id="stop-[0-9]"' index.html                                  # 6
rg -c '<meta name="robots" content="noindex, nofollow">' index.html          # 1
rg -c 'Jay Z' index.html                                                     # 1
! rg -qi 'jarda|thebestviewpoints|korunaevropy|jardaphoto' index.html
```

## Commit template

```
feat(C7.6): Block F+ bridge — gear-content pointer

Adds a single <p class="research-bridge"> paragraph between Block E
(growth-opportunities) and Block F (seo-recipes). Points at the
gear-lists research bundle (gear-lists-business-research.md) without
inlining it — keeps the primer lean.

Names Bergfreunde.eu + Amazon DE as the recommended starter affiliate
stack. NOT a .research-block — block count stays at 7.

Adds .research-bridge CSS (bg-inset background, muted left border,
inline <code> styling for the file path). Responsive @ 480px.

PRD: C7.6
Codex-reviewed: <folded SHA>
```

## No-gos

- No class="research-block" on the bridge (would push block count to 8 — invariant break).
- No deep-diving gear content — the research-bundle file holds depth; bridge only points.
- No third affiliate program recommendation (two is the commitment; bridging more is out of scope).
- No reader's URLs.
- No links to the research-bundle file (path-reference only, since the primer is single-file and the research bundle is gitignored in public).

## Known risks + mitigations

1. **Bridge paragraph feels like an aside inside the appendix** — by design. The `.research-bridge` CSS treats it visually as a quoted pointer (indented, border-left), so readers don't mistake it for a research block itself.
2. **Affiliate program drift** — Bergfreunde.eu and Amazon DE are current as of research date. If either shuts down or changes terms, the mention drifts. Not urgent; research-bundle file is the authoritative source.
3. **Code path in prose** — "`docs/research/2026-04-21-round-2-research/gear-lists-business-research.md`" is long; responsive CSS breaks it with `word-break: break-all` at narrow widths. Tradeoff: authenticity vs. readability. Chose authenticity.
