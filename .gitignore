# === Private customized versions (never commit the real-named HTML) ===
index.private.html
*.private.*
*.private
private/
private-*
customized/
custom-*
*.custom.*
personal/
*.personal.*

# === Internal planning + research (real name / URLs / referral codes live here) ===
docs/plans/
docs/research/
docs/verification/*.json
contracts/
# research/ dir: reshape 2026-04-24 converted this into the public research-dossier
# surface (market-clusters.html etc.). HTML pages here ship; anything non-HTML
# stays gitignored by targeted rules below.
research/*.md
research/*.json
research/*.txt
research/private/
research/draft/

# === Environment ===
.env
.env.local
.env.*.local
*.env
*.env.*

# === Potential leak files ===
*.pdf
*.docx
*.doc
*.pages
*.key
*.csv
*.xlsx
*.xls
screenshot*.png
screenshot*.jpg
Screen*Shot*.png

# === Lighthouse / audit outputs ===
lighthouse-*.html
lighthouse-*.json
.lighthouseci/

# === OS + editor ===
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*~

# === Node / build artifacts (future-proofing) ===
node_modules/
dist/
build/

# === qa-lint artifacts (regenerated on each run) ===
.qa-reports/
